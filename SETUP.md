# Інструкції з Налаштування

## ✅ Завершено: Базова Інфраструктура

Базова інфраструктура онлайн школи навчання успішно налаштована та протестована.

### 🏗 Що Створено

#### Backend (Node.js)
- ✅ Express.js сервер з middleware безпеки
- ✅ PostgreSQL підключення та схема БД
- ✅ Redis кешування
- ✅ JWT аутентифікація (заготовка)
- ✅ Логування з Winston
- ✅ Обробка помилок
- ✅ CORS та Rate Limiting
- ✅ Health Check endpoint

#### Frontend (React)
- ✅ React 18 з hooks
- ✅ Redux Toolkit для стану
- ✅ React Router для навігації
- ✅ Styled Components для стилів
- ✅ Framer Motion для анімацій
- ✅ Адаптивний дизайн
- ✅ PWA готовність

#### DevOps
- ✅ Docker контейнеризація
- ✅ Docker Compose для розробки
- ✅ Nginx конфігурація
- ✅ Змінні середовища
- ✅ Структура проекту

#### Тестування
- ✅ Unit тести для backend (Jest)
- ✅ Unit тести для frontend (React Testing Library)
- ✅ Property-based тести (fast-check)
- ✅ Тести конфігурації та middleware

### 🚀 Швидкий Запуск

#### З Docker (Рекомендовано)
```bash
# Запуск всіх сервісів
docker-compose up -d

# Перевірка статусу
docker-compose ps

# Перегляд логів
docker-compose logs -f
```

#### Локальна Розробка
```bash
# Backend
cd backend
npm install
npm run dev

# Frontend (в новому терміналі)
cd frontend
npm install
npm start
```

### 🌐 Доступ до Сервісів

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:4000
- **Health Check**: http://localhost:4000/health
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379

### 🧪 Запуск Тестів

```bash
# Backend тести
cd backend && npm test

# Frontend тести
cd frontend && npm test

# Property-based тести
cd frontend && npm test -- --testNamePattern="Property Test"
```

### 📊 Результати Тестування

#### Backend
- ✅ Конфігураційні тести: 8/8 пройдено
- ✅ Middleware тести: 6/6 пройдено
- ⚠️ Database/Redis тести: потребують запущених сервісів

#### Frontend
- ✅ App компонент тести: 6/6 пройдено
- ✅ Redux store тести: 17/17 пройдено
- ✅ Property тести адаптивності: 4/4 пройдено (400 ітерацій)

### 🔧 Налаштування Середовища

1. **Скопіюйте змінні середовища:**
   ```bash
   cp backend/.env.example backend/.env
   ```

2. **Налаштуйте базу даних** (якщо не використовуєте Docker):
   - Створіть PostgreSQL базу `learning_school`
   - Запустіть `database/init.sql`

3. **Налаштуйте Redis** (якщо не використовуєте Docker):
   - Встановіть та запустіть Redis сервер

### 📁 Структура Проекту

```
online-learning-school/
├── backend/                 # Node.js API
│   ├── src/
│   │   ├── database/       # PostgreSQL підключення
│   │   ├── cache/          # Redis кешування
│   │   ├── routes/         # API маршрути
│   │   ├── middleware/     # Express middleware
│   │   ├── utils/          # Утиліти (логер)
│   │   └── __tests__/      # Тести
│   ├── logs/               # Файли логів
│   └── package.json
├── frontend/               # React додаток
│   ├── src/
│   │   ├── components/     # React компоненти
│   │   ├── pages/          # Сторінки
│   │   ├── store/          # Redux store
│   │   └── __tests__/      # Тести
│   └── package.json
├── database/               # SQL схеми
├── nginx/                  # Nginx конфігурація
├── docker-compose.yml      # Docker Compose
└── README.md
```

### 🎯 Наступні Кроки

Базова інфраструктура готова для реалізації наступних завдань:

1. **Завдання 2**: Система аутентифікації та авторизації
2. **Завдання 3**: Навігація та UI компоненти
3. **Завдання 4**: Система управління контентом
4. **Завдання 5**: Спеціальні сторінки контенту

### 🔍 Перевірка Роботи

Після запуску перейдіть на http://localhost:3000 та переконайтеся, що:
- ✅ Сторінка завантажується
- ✅ Відображається статус інфраструктури
- ✅ Навігаційне меню працює
- ✅ Health check доступний на /health

### 🐛 Усунення Проблем

**Проблема**: Docker контейнери не запускаються
**Рішення**: Переконайтеся, що Docker Desktop запущений

**Проблема**: Порти зайняті
**Рішення**: Змініть порти в docker-compose.yml або зупиніть конфліктуючі сервіси

**Проблема**: Тести не проходять
**Рішення**: Переконайтеся, що залежності встановлені (`npm install`)

---

**Статус**: ✅ Завершено  
**Тестування**: ✅ Пройдено (37/37 тестів)  
**Готовність**: 🚀 Готово до наступних завдань